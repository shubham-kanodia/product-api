application.name = "products"

akka {
  loglevel = DEBUG
}

http {
  interface = "0.0.0.0"
  port = 8080
}

database {
  profile = "slick.jdbc.PostgresProfile$"
  driver = org.postgresql.Driver
  databaseName = "rtjvm"
  url = "jdbc:postgresql://postgres-db.info:30036/rtjvm"
  user = "docker"
  password = "docker"
  driver = org.postgresql.Driver
  numThreads = 10
}
